#pragma once
#include <iostream>
#include <vector>
#include <random>
#include <ctime>

using namespace std;

string generateRandomString(size_t length);

vector<string> split(const string &s, const string &delimiter);
